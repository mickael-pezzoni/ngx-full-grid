import { ValueFromPropertyPipe } from './value-from-property.pipe';

describe('ValueFromPropertyPipe', () => {
  it('create an instance', () => {
    const pipe = new ValueFromPropertyPipe();
    expect(pipe).toBeTruthy();
  });
});
